# <프로젝트명> — Claude Instructions

<프로젝트 한 줄 설명>. <위험도가 있으면 명시 — 예: 실기 하드웨어/운영 DB 연결 프로젝트, 미승인 변경 위험>

- 환경: <OS / 언어 / 프레임워크 / 컨테이너 여부와 이름>
- 브랜치: 모든 작업은 `<기본 브랜치>`에서 진행. **CLAUDE.md는 커밋·푸시 금지** (.gitignore에 `CLAUDE.md`와 `.claude/` 등록).

## 절대 규칙

1. **수정 전 보고·승인.** 버그 수정·동작 변경은 ① 증상 ② 기대 동작 ③ 원인 위치(파일:라인) ④ 수정 방향을 먼저 보고하고 승인받은 후 진행. 확신이 없으면 "보류"로 기록만 한다.
2. **Surgical Change.** 요청받은 것만 고친다. 무관한 코드·주석·포매팅 수정 금지, 요청하지 않은 기능·리팩토링·예외처리 추가 금지.
3. **실행 검증은 사용자 몫.** <임의 실행 금지 대상 — 예: 서버 기동, 배포, 실기 구동>은 사용자가 직접 한다.
4. 수정은 diff로 제시. 추측은 추측이라고 명시.

## 검증 명령 (코드 수정 후 필수)

```bash
# <실행 위치: 호스트 or 컨테이너>
<린트 명령 — 예: ruff check . / eslint src/>
<타입·컴파일 체크 — 예: python3 -m py_compile <file> / tsc --noEmit>
<테스트 — 예: pytest tests/>
```

- <알려진 오탐이 있으면 여기에 — 예: cv2 E1101은 무시>

## 같이 고쳐야 깨지지 않는 값 (SSoT)

<코드만 읽어서는 알 수 없는 파일 간 결합만 적는다. 코드에서 파악 가능한 구조 설명은 쓰지 않는다.>

- <예: 상태 enum의 발원지는 A파일 — 표시 상태 변경 시 B·C파일 동시 수정>
- <예: 설정 기본값은 X파일 ↔ Y파일 일치 유지>

## 코딩 규칙

<언어 기본값과 다른 것만 적는다. "깨끗한 코드 작성" 같은 자명한 규칙 금지.>

- 주석은 한국어. 코드가 말하지 못하는 제약·근거만 적는다.
- 파일명·식별자 변경 전 사용처 grep 필수.
- <프로젝트 특수 규칙 — 예: 특정 라이브러리 버전 핀과 그 이유>

## 응답 규칙

- 한국어. 이모티콘·인사말·자화자찬 금지. 보고는 확인된 사실만 간결하게.
- 명령 제시 전 실행 위치 명시 (호스트 vs 컨테이너, 작업 디렉토리).
- 다단계 작업은 계획 제시 → 승인 후 실행. 모호하면 질문하고, 가정하고 진행할 땐 가정을 명시.

## 위임 (오케스트레이션)

메인 에이전트는 계획·분배·결과 종합을 맡고, 실행은 서브에이전트에 위임한다. 훅이 메인 에이전트의 코드 파일 직접 수정을 한 턴에 3개까지만 허용 (3파일 SSoT 세트 직접 처리 가능) — 차단되면 Bash(sed·리다이렉트 등)로 우회하지 말고 위임한다.

**재위임 금지: 아래 위임 기준은 메인 에이전트 전용이다. 서브에이전트로 실행 중이라면(위임받은 작업이라면) 다른 에이전트를 스폰하지 말고 받은 작업을 직접 수행한다.** (서브에이전트가 이 기준을 자기에게 적용해 재귀 위임 체인이 생긴 사고가 있었다 — 2026-07-05.) 게이트 훅이 서브에이전트의 Agent/Task 호출을 물리 차단한다.

- 새 기능·4개 이상 파일·50줄 이상 변경·테스트 작성·리팩토링 → **default-worker** (Sonnet)
- 아키텍처 판단·까다로운 원인 분석 → **deep-reasoner** (Opus). 단 메인 모델 자체가 Opus면 메인이 직접 수행하고, deep-reasoner는 병렬 관점·교차 검증용으로만 쓴다.
- grep 조사·로그 확인·기계적 반복 편집 → **task-worker** (Haiku)
- 3개 이하 파일 소규모 수정·문서·설정·단순 질문은 직접 처리.

### 메인 모델 티어별 운영

메인 모델이 무엇이든 아래를 따른다:

- **메인 = Sonnet**: 아키텍처 판단·까다로운 원인 분석·위험(실기/운영) 관련 변경은 직접 결론 내지 말고 **deep-reasoner(Opus)에 위임하거나 교차 검증**을 받는다. 검증 없이 확신이 서지 않으면 "보류"로 보고. 구현 위임 기준은 위와 동일.
- **메인 = Opus**: 추론·검토는 직접, 50줄 이상 구현은 default-worker(Sonnet)로 위임해 비용을 아낀다. deep-reasoner는 교차 검증용으로만.
- **공통 절약 규칙**: 조사·grep·로그 확인은 항상 task-worker(Haiku) 우선. 큰 파일은 전문 Read 대신 offset/limit·grep으로 필요한 부분만. 서브에이전트 반환은 결론+파일:라인만 요구하고 파일 전문 인용을 금지한다.

커스텀 서브에이전트(위 3종)는 CLAUDE.md를 자동 로드한다. 그래도 위임 프롬프트에 해당 작업의 핵심만 다시 짚는다: 관련 SSoT, 대상 파일 경로, 반환 형식(`수정 파일 / diff / 검증 결과 / 남은 우려사항`). 단 **내장 Explore/Plan 에이전트는 CLAUDE.md를 로드하지 않으므로** 필요한 규칙을 프롬프트에 직접 넣는다.

세부 기준: @.claude/rules/orchestration.md
