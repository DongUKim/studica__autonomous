# 플랜 B — 주행기록(Teach & Repeat) 네비게이션 구현 스펙 (Claude Code용)

> **위치**: 본 문서는 3계층 하이브리드 네비게이션(플랜 A)이 실기에서 목표 성능에 도달하지 못할 경우의
> **차선책(플랜 B)** 스펙이다. `ROS2_패키지_아키텍처.md`(이하 아키텍처 문서)의 패키지 구조에
> **플러그인 형태로 삽입**되며, 오케스트레이터·플래너·비전·베이스는 그대로 재사용한다.
> 플랜 A 문서들과 충돌 시 플랜 B 실행 중에는 본 문서가 우선한다.

---

## 0. 발동 기준과 플랜 A와의 관계

### 0.1 전환 결정 게이트 (사전에 합의된 기준으로만 전환)

다음 중 하나가 확정되면 플랜 B로 전환한다(감이 아니라 수락시험 결과로 판단):

| 게이트 | 기준 |
|---|---|
| G1 | 실기에서 네비 문서 §11.3 수락시험 3(1 m 정사각 왕복 ≤5 cm/3°)·4(벽보정 수렴)를 **튜닝 5일 이후에도** 통과 못함 |
| G2 | 호모그래피/패드 검출이 대회장 환경에서 신뢰도 미달(패드 리셋 오보정 반복) — 단, 이 경우 L1+벽보정은 유지하고 패드 리셋만 대체하는 **부분 전환**을 먼저 검토 |
| G3 | 대회 D-7까지 플랜 A 마일스톤 8(sim 풀미션) 미달 |

부분 전환 원칙: 플랜 A/B는 전부 아니면 전무가 아니다. 문제인 계층만 바꾼다
(예: 주행은 티치&리피트로, 스테이션 정렬은 라인트레이서 유지).

### 0.2 공유 자산 (플랜 B의 신규 개발 범위를 최소화)

| 그대로 재사용 | 대체됨 |
|---|---|
| agv_base(안전가드 포함), agv_camera, agv_sim, agv_interfaces | agv_nav의 **지도 기반 목표·보정**(go_to 목표 좌표, 벽보정 예측, 패드 리셋) |
| **추정기(데드레코닝 부분)** — 기록·재생 양쪽의 기준 | field.yaml 좌표 의존 주행 |
| agv_line_tracer(작동한다면 스테이션 정렬에 계속 사용) | |
| agv_ball_vision, agv_barcode, agv_planner(그래프 가중치만 교체), agv_master 오케스트레이터 | |

**신규는 패키지 1개**: `agv_repeat` — 기록기(teleop 확장) + 재생 제어기 + 검증 모드.

---

## 1. 핵심 설계 — "명령 재생"이 아니라 "기록된 경로 추종"

기록하는 것은 텔레옵 중 추정기가 계산한 **경로(pose 궤적)와 센서 서명**이고,
재생하는 것은 그 경로를 **50 Hz 폐루프 경로추종 제어기**로 따라가는 것이다.
기록된 cmd_vel을 그대로 다시 내보내는 방식(개루프)은 슬립·전압·적재무게 변화로 반드시 발산하므로
채택하지 않는다. cmd_vel은 재생 시 **매 주기 오차로부터 새로 계산되어 토픽으로 발행**된다.

### 사용자 요구 → 설계 매핑

| 요구 | 설계 반영 |
|---|---|
| 키보드 텔레옵으로 기록 | §3 기록기(teleop 확장 + HUD + 세그먼트 자동 분할) |
| IMU·엔코더·초음파·PSD 기록, **PSD는 30 cm 이내만** | §2 샘플 스키마의 `valid` 플래그 — PSD는 `d < 0.30 m`일 때만 유효 기록 |
| 기록 기반 자율주행 | §4 재생 제어기 — 기록 경로에 대한 횡오차/헤딩오차 폐루프 추종 |
| `time.sleep()` 금지, cmd_vel 토픽 구동 | 진행변수 = **시간이 아니라 경로 호장(arc length) s**. 50 Hz 타이머에서 s 기준 목표점을 뽑아 /cmd_vel 발행 |
| 회전 중 초음파·PSD 오염 | 이중 차단: **기록 시** `|wz|>0.1 rad/s`(+정지 후 0.3 s) 구간 무효 플래그, **재생 시**에도 회전 중 서명 보정 미적용. 회전은 IMU 절대 yaw 폐루프로만 수행(§4.3) |

---

## 2. 데이터 모델

### 2.1 구조: 레그(leg) → 세그먼트(segment) → 샘플(sample)

- **레그** = 체크포인트(스테이션/분기점) 사이의 1회 이동 기록. 파일 1개.
  체크포인트 집합은 플랜 A의 웨이포인트 그래프 노드를 그대로 쓴다 —
  **스테이션 쌍 전수(≈130개)가 아니라 그래프 엣지(≤16개)만 티칭**하고,
  임의 구간 이동은 엣지 재생의 연쇄로 조립한다(§5.1).
- **세그먼트** = 기록기가 자동 분할한 구간. `T`(병진) 또는 `R`(회전).
  판정: `|wz| > 0.1 rad/s`이고 병진속도 < 0.05 m/s가 0.3 s 지속 → `R`, 그 외 `T`.
  병진+회전 동시 조작은 기록기가 경고(§3.3 운전 수칙).
- **샘플** = 50 Hz 기록 단위.

### 2.2 파일 배치·포맷

```
missions/<name>/taught_legs/
├── index.yaml                 # 레그 목록: from, to, 길이, 소요, 기록일, 파일, 버전
├── N1__N2.jsonl               # 최신 버전 (이전 버전은 N1__N2.v1.jsonl로 보존, 최근 3개)
└── ...
```

JSON Lines(1행=1샘플). 첫 행은 메타(`{"meta": {from, to, date, robot_cfg_hash}}`).
엑셀 검토용 CSV 변환기(`tools/leg_to_csv.py`)를 함께 제공한다(사용자 선호 포맷 병행 지원).

```json
{"t": 1.24, "seg": "T", "x": 0.412, "y": 0.008, "th": 0.004,
 "v": 0.31, "wz": 0.00, "enc": [1032, 1029, 1030, 1031], "yaw": 0.004,
 "us_l": 0.412, "us_r": 1.180, "psd_f": null, "psd_l": 0.148, "psd_r": null,
 "valid": {"us_l": true, "us_r": true, "psd_l": true}}
```

규칙: (1) `x,y,th`는 **레그 시작 pose를 원점으로 하는 레그-로컬 프레임**(레그 조립성 확보),
단 `yaw`는 IMU 절대값도 병기. (2) PSD는 측정치가 0.30 m 이상이면 `null` + valid=false
(장거리 편차 배제 — 사용자 요구). (3) `R` 세그먼트와 그 종료 후 0.3 s 동안 모든 거리채널
valid=false. (4) `t`는 분석용일 뿐 재생 로직은 참조 금지. (5) cmd_vel도 기록하되(분석·속도
프로파일 힌트) 재생에 직접 사용 금지.

로드 시 컴파일: `T` 세그먼트는 호장 2 cm 간격 폴리라인 + θ(s) + v(s) 프로파일로 리샘플,
`R` 세그먼트는 (시작 yaw, 종료 yaw, 방향)으로 축약. 유효 센서 서명은 s 인덱스로 정렬.

---

## 3. 기록기 (`agv_repeat` teach 모드)

### 3.1 실행

`ros2 launch agv_master teach_repeat.launch.py mission:=mission_a` —
base, camera, **추정기(데드레코닝, 지도 보정 off)**, teleop, 기록기 노드 기동.

### 3.2 조작·HUD

- teleop: WASD=병진(vx, vy), Q/E=회전(**기본 잠금**, 모디파이어 키로만 활성 — 병진/회전 분리 유도).
- 기록 제어: `[` 시작(현재 체크포인트 → 목표 체크포인트를 index에서 선택),
  `]` 종료·저장, `Esc` 폐기, `r` 해당 레그 재기록(버전 보존).
- HUD(터미널): 현재 레그 from→to, 세그먼트 자동판정(T/R), 경과 호장, 유효 센서 채널 표시
  (PSD가 30 cm 게이트 안인지 실시간 확인), 회전 오염 차단 상태, 저장 샘플 수.
- 서비스 인터페이스(자동화·테스트용): `/repeat/record_start {from, to}`, `/repeat/record_stop {save}`.

### 3.3 운전 수칙 (HUD에 상시 표기)

1. 레그 주행은 가급적 **병진만으로**(메카넘) — 회전은 스테이션 정렬 등 꼭 필요한 곳에서만.
2. 보정 존(벽 근처 직선 구간)에서는 **벽에서 15~25 cm를 유지**하며 통과 — PSD 30 cm 게이트 안에
   들어와야 서명이 기록되고, 그 서명이 재생 시 드리프트를 깎는 앵커가 된다.
3. 레그 시작·종료는 정지 상태에서. 종료 지점 = 다음 레그 시작 지점(체크포인트 일관성).
4. 속도는 재생 상한이 된다 — 최종 티칭 전 1회는 목표 경기 속도로 주행.

---

## 4. 재생 제어기 (`agv_repeat` repeat 모드)

### 4.1 레그 앵커링

레그 k 재생 시작 시, 기록된 레그-로컬 경로를 **로봇의 현재 pose에 강체변환으로 접합**한다
(기록 시작 pose ↔ 현재 pose 정합). 출발 스테이션 거치 지그로 초기 자세를 재현하고,
이후 체크포인트마다 새로 앵커되므로 오차가 경로 변형으로 누적되지 않는다.
yaw 정합은 IMU 절대값 기준(지그 출발이면 회전 오프셋 ≈ 0, 그래도 항상 계산·적용).

### 4.2 `T`(병진) 세그먼트 — 경로추종

50 Hz 매 주기:
1. 현재 데드레코닝 pose를 티칭 폴리라인에 사영 → 진행 호장 `s_now`, 횡오차 `e_lat`,
   헤딩오차 `e_th = θ_taught(s_now+lookahead) − θ_now`. (lookahead = 6 cm)
2. 명령 생성 → /cmd_vel 발행:
   - `vx` = min(기록 속도프로파일 v(s), vmax) — 세그먼트 말단은 감속식 `v ≤ √(2·a·s_remain)`
   - `vy` = −K_lat · e_lat (K_lat = 1.5 /s, 메카넘 횡보정 주력)
   - `wz` = K_th · e_th (K_th = 2.0 /s, IMU 기반 헤딩 유지)
3. 종료: `s_now ≥ s_end` 그리고 종점까지 유클리드 오차 < 20 mm, 0.2 s 디바운스 → 다음 세그먼트.
4. 이탈 감시: `|e_lat| > 80 mm` 0.5 s 지속 → 정지, 액션 abort(오케스트레이터 리커버리로).

**서명 보정(드리프트 트리머)** — 티칭 때 유효 기록이 있는 s 구간에서만:
- 측면 PSD/초음파: `res = 현재측정 − 티칭측정(s_now)` → `e_lat` 추정을 `K=0.3`으로 갱신.
- 전방 PSD: 같은 방식으로 **s_now(종방향)**를 갱신.
- 게이트: 채널 valid(티칭·현재 모두), `|res| < 0.15 m`, `|wz| < 0.1 rad/s`(회전 중 미반영 —
  사용자 요구), 연속 5샘플 미디언. 한 채널은 한 축만 보정.

### 4.3 `R`(회전) 세그먼트 — IMU 단독

목표 = 기록 종료 yaw(앵커 회전 오프셋 반영). `wz = K_yaw · wrap(err)`, 상한 1.2 rad/s,
종료 `|err| < 1°` 0.3 s. **거리센서는 판독·보정 모두 미사용**(사용자 요구). 병진 성분 0.

### 4.4 보조 기동

`RelativeMove(dx, dy, max 0.3 m)` — 스테이션 후퇴·바코드 재정렬용 단거리 데드레코닝 이동.
플랜 A의 "0.3 m 이내 개루프 허용" 경계 규칙을 그대로 계승한다.

---

## 5. 미션 통합

### 5.1 오케스트레이터 무변경 통합

- `agv_repeat`는 **`/nav/follow_route` 액션(FollowRoute 타입)을 동일 이름으로 서빙**한다.
  goal의 노드 열을 받아 index.yaml에서 엣지 레그를 찾아 순차 재생. 누락 엣지는 즉시 abort +
  "미티칭 엣지" 명시(티칭 체크리스트로 연결).
- launch에서 agv_nav motion_node ↔ agv_repeat repeat_node만 치환:
  `competition.launch.py nav_backend:=repeat`. 오케스트레이터·페이즈 FSM은 수정 0.
- `GoToPose`(자유 좌표)는 플랜 B에서 미지원 — 오케스트레이터는 후퇴·미세이동을
  `RelativeMove`로 호출(플랜 A에서도 동일 인터페이스로 통일해 분기 제거).

### 5.2 플래너 재사용

티칭 엣지 목록(index.yaml)이 곧 그래프: 노드 = 체크포인트, 가중치 = 티칭 호장 길이.
agv_planner는 FieldMap 그래프 대신 이 그래프를 로드하는 파라미터 하나로 동작
(`graph_source: field | taught`). 순서 전략·시간예산 컷 로직 동일.

### 5.3 스테이션 접근 — 두 경우

- **라인트레이서 가용**(권장): 레그 종점 = 스테이션 접근노드 → AlignAndTrace로 최종 진입.
  플랜 A와 동일한 정밀 정렬 품질 확보.
- **라인트레이서 불가**: 접근 자체를 티칭에 포함 — 스테이션 직전 감속 도킹 서브세그먼트
  (v ≤ 0.08 m/s)까지 기록하고, 측면 서명 보정이 걸리는 경로로 티칭(§3.3-2).
  이 경우 최종 정밀도는 매니퓰레이터의 기구적 관용(V홈 등)에 의존함을 명시.

바코드·공 색상·매니퓰레이션·램프·시간예산·스킵 정책: 아키텍처 문서 그대로.

---

## 6. 검증 모드 (`--mode validate_repeat`)

전 레그(또는 지정 목록)를 매니퓰레이션 없이 연속 재생하며 기록:
- `tracking.csv`: t, leg, seg, s, e_lat, e_th, 적용된 서명보정량
- 리포트: 레그별 `e_lat` 95퍼센타일 / 종점 도달 오차 / 서명 잔차 통계 / 소요시간 vs 티칭시간
- 합격 기준(초기값, 실기 튜닝으로 조정): 전 레그 e_lat p95 ≤ 25 mm, 종점 오차 ≤ 15 mm
  (라인트레이서 병용 시 ≤ 25 mm 허용), 총 소요 ≤ 시간예산.
- 진단 힌트: 특정 레그만 불량 → 그 레그 재티칭 / 전 레그 동일 방향 편향 → 지그·IMU 캘리브레이션 /
  서명 잔차 급증 → 코트 변경(재티칭 필요) 탐지.

---

## 7. 시뮬레이션·테스트

- agv_sim 그대로: sim에서 teleop으로 티칭 → 재생 → 검증 리포트까지 전 사이클 무실기 수행.
  노이즈 파라미터를 올려(슬립 2%, IMU 드리프트 1°/min) 강건성 확인.
- 코어 로직 pytest(ROS 무의존): 세그먼트 분할기, 폴리라인 리샘플·사영(s, e_lat), 앵커 변환,
  서명 보정 게이트, 감속 프로파일, index 로더.
- 기록 재현 테스트: 티칭 파일을 고정 입력으로 재생 제어기 회귀 테스트(bag 불필요).

---

## 8. 마일스톤 (플랜 A와 병행 가능한 최소 비용 경로)

| # | 내용 | 수락 |
|---|---|---|
| R1 | 데이터 모델 + 기록기(teleop 확장, HUD, 게이트 플래그) | sim 티칭 → JSONL 스키마 검증, CSV 변환 |
| R2 | 재생 제어기 T/R + 앵커링 + FollowRoute 서빙 | sim: 노이즈 하 단일 레그 e_lat p95 ≤ 25 mm |
| R3 | 서명 보정 + 이탈 감시 + RelativeMove | sim: 드리프트 주입 후 보정 존 통과 시 오차 회복 |
| R4 | 검증 모드 + 리포트 + 플래너 graph_source 연동 | sim 전 레그 연속 재생 리포트 PASS |
| R5 | 실기 튜닝 + 지그 제작 + 티칭 운용 리허설 | 실코트(연습장) 검증 리포트 PASS, 풀미션 1회 |

R1~R4는 sim만으로 완결되므로 플랜 A 개발과 충돌 없이 선행 구축해 둘 수 있다(보험 가치).

---

## 9. 한계·리스크와 운용 수칙 (정직한 전제)

1. **절대 좌표 리셋이 없다**: 서명 보정은 벽 근처에서만 작동. 긴 개활 구간은 드리프트가 그대로
   남는다 → 라인트레이서 병용이 사실상 필수 권장.
2. **출발 재현성**: 스테이션 거치 지그 필수(±2 mm/±0.5° 목표). 지그 없으면 전 레그가 함께 틀어진다.
3. **재티칭 비용**: 과제 30% 변경 시 영향 엣지 재티칭. 엣지 수 ≤ 16 유지, 엣지당 2~3분 →
   전체 재티칭 ≤ 45분을 당일 시간표에 반영(부분 변경은 해당 엣지만).
4. 평가코트 2개 운영 시 코트별 티칭 세트 분리(`taught_legs_courtA/` 등) — index에 코트 태그.
5. 배터리·적재 무게 변화는 경로추종이 흡수하지만 속도 상한 여유 20%를 남겨 티칭할 것.

---

## 10. 확인 사항

1. 체크포인트(그래프 노드) 확정안 — 플랜 A 웨이포인트와 동일하게 갈지, 티칭 편의로 조정할지.
2. 출발 지그 제작 가능 여부·방식(스테이션 모서리 맞댐 등).
3. 키보드 teleop 속도 단계(저속/중속/고속 프리셋) 선호.
4. 라인트레이서를 플랜 B에서도 사용하는 것으로 확정할지(§5.3 분기 — 강력 권장).
5. 티칭 파일 보존 정책(버전 수, 코트별 세트) 및 대회 당일 티칭 시간 배정.
